<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sbk.camping">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/camping"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".git.GitDetayActivity"
            android:parentActivityName=".git.GitActivity" />
        <activity
            android:name=".git.GitActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".kamp.malzeme.KampMalzemeListActivity"
            android:parentActivityName=".kamp.KampListActivity" />
        <activity
            android:name=".kamp.KampListActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".malzeme.MalzemeListActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>